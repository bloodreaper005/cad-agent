interface:
  display_name: "Mechanical Design"
  short_description: "Build, validate, and learn from mechanical CAD"
  default_prompt: "Use $mech-cad-design to turn this requirement into a validated FreeCAD model."
policy:
  allow_implicit_invocation: true
