.env.local
.venv/
.worktrees/
__pycache__/
*.pyc
.pytest_cache/
.coverage
data/
/designs/
/output/
/dist/
# Generated CAD artifacts. FreeCAD writes documents into the working directory,
# so an interactive session can drop one into the repository root.
*.FCStd
*.FCStd[0-9]
*.step
*.stp
*.brp

# Internal planning and spec documents. public-repository.toml already
# classifies this as private; ignoring it keeps a locally recreated copy
# from being committed back into a now-public repository.
docs/superpowers/
